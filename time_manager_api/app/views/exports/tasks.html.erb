<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Tasks export <%= date_range %></title>
  </head>
  <body>
    <h1><%= date_range %></h1>

    <% groups.each do |group| %>
      <h3><%= group.date %> - <%= group.total_time %></h3>

      <ul>
        <% group.tasks.each do |task| %>
          <li><%= task.description %></li>
        <% end %>
      </ul>
    <% end %>
  </body>
</html>
